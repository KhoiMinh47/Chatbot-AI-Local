[project]
name = "ntc-rag-eval"
version = "0.1.0"
description = "Reproducible retrieval metrics and calibration for the NTC RAG benchmark"
requires-python = ">=3.14,<3.15"
dependencies = []

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ntc_rag_eval"]
